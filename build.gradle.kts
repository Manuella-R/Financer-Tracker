// Project-level build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        // This is the correct location for your AGP classpath
        classpath ("com.android.tools.build:gradle:8.6.0")  // Make sure this is correct
    }
}
